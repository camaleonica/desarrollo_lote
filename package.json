{
  "name": "desarrollo_lote",
  "private": true,
  "scripts": {
    "start": "npm start --prefix mobile",
    "mobile": "npm start --prefix mobile",
    "backend": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "setup:db": "npm run setup:db --prefix backend"
  }
}
